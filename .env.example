# Render / local environment
PORT=10000
NODE_ENV=production

# Base64 session handling. Do NOT commit real credentials.
SESSION_PREFIX=lmk-md

# Optional public URL used only where the application explicitly needs it.
# PUBLIC_URL=https://your-service.onrender.com
