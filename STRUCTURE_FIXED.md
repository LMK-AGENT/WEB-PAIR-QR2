# Cleaned project structure

This archive removes the accidentally nested duplicate `Arslan-Pairing-Site-main`
project and excludes runtime `temp/` authentication artifacts.

Deploy the contents of this directory as the root of the Render Web Service.
Do not commit real WhatsApp sessions or `.env` files.
